#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Revisión automática de código después de push
# Solo se ejecuta si hay cambios en archivos de código
if [ -f "scripts/auto-code-reviewer.js" ]; then
  echo ""
  echo "🔍 Ejecutando revisión automática de código post-push..."
  node scripts/auto-code-reviewer.js --full || true
fi

